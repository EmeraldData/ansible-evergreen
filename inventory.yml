---
all:
  vars:
    ansible_connection: local
    ansible_user: emerald
    ansible_ssh_private_key_file: /home/emerald/.ssh/id_rsa
    ansible_sudo_executable: /usr/bin/sudo

    mounts:
      - name: /openils/var/web/reporter
      
  children: 
    prod_hosts:
      hosts:
        localhost:
